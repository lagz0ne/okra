# General Project Memory

Objective: `learning_memory_reuse_acceptance_rate >= 0.85`.

Previous runs are automatically scanned. The notes include traps, avoidances, misconceptions,
optimizations, and candidate anti-goals with metric thresholds and human ratification status.

Stress means time and DKR budget pressure. The orchestrator allocates DKR as the learning allocator,
opens cannot when the objective is unachievable, and repairs with cannot, breaking, pointless, and
authority_drift flags.

Evidence includes hashes, deterministic eval output, append-only records, independent review,
`eval_regression_count == 0`, and `single_llm_truth_acceptance_count == 0`.

This is only a generic assistant memory log.
