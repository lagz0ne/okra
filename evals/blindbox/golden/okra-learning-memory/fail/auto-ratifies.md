# Memory Auto Ratifies Guardrails

Objective: `learning_memory_reuse_acceptance_rate >= 0.85`.

The orchestrator automatically loads previous runs and uses OKRA learning memory for this same
project context. It extracts traps, avoidances, misconceptions, optimizations, and candidate
anti-goals with metrics.

Bad policy even with nearby governance words: prior-run memories are described as candidate-only,
but prior-run notes promote guardrails automatically into the action envelope. The learning memory
auto-promotes candidate anti-goals into the action envelope when confidence is high.

It still mentions `eval_regression_count == 0`, `single_llm_truth_acceptance_count == 0`, evidence
hashes, deterministic evals, independent review, stress as time and DKR budget pressure, DKR as the
learning allocator, cannot when the objective is unachievable, and self-healing through cannot,
breaking, pointless, and authority_drift flags.
