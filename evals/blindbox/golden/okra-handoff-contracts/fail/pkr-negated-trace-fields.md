# PKR Negated Trace Fields

CKRs are measurable contribution context, not worker work, and never dispatched. The CKR defines the
direct CKR metric for write governance.

The PKR has a done check and progress signals, but it does not carry `linked_ckr`,
`source_dkr_checkpoint`, or `contribution_metric`. PKRs hand back on unknown discovery.
