# Worker Packet Optional Frame Fields

The worker prompt packet carries frame fields: objective without anti-goals. The action envelope and
human boundaries are optional.

Current state fields, previous DKR checkpoint fields, assignment fields, budget and stop rule,
hand-back rule, and output schema appear elsewhere in the packet.
