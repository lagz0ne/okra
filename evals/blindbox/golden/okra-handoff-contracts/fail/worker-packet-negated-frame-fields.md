# Worker Packet Negated Frame Fields

The worker prompt packet carries objective only. Anti-goals, action envelope, and human boundaries
are not included in the packet.

Current state fields, previous DKR checkpoint fields, assignment fields, budget and stop rule,
hand-back rule, and output schema appear elsewhere in the packet.
