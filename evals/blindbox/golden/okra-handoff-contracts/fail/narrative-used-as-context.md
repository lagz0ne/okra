# Narrative Used As Context

Worker progress in `progress.jsonl`, check-ins, metric reads, flags, and accepted checkpoints
influence the next dispatch. Freeform narrative is not evidence, but it can be used as context for
the next dispatch.
