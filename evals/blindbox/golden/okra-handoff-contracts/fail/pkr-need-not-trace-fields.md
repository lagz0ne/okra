# PKR Need Not Trace Fields

CKRs are measurable contribution context, not worker work, and never dispatched. The CKR defines the
direct CKR metric for write governance.

PKRs need not carry `linked_ckr`, `source_dkr_checkpoint`, or `contribution_metric`. They still have
a done check, progress signals, and a hand-back rule.
