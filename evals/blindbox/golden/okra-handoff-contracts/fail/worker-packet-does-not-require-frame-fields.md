# Worker Packet Does Not Require Frame Fields

Worker prompt packet required fields include objective, but do not require anti_goals,
action_envelope, or human boundaries.

Current state fields, previous DKR checkpoint fields, assignment fields, budget and stop rule,
hand-back rule, and output schema appear elsewhere in the packet.
