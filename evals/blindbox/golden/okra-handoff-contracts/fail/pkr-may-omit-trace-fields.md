# PKR May Omit Trace Fields

CKRs are measurable contribution context, not worker work, and never dispatched. The CKR defines the
direct CKR metric for write governance.

PKRs may omit `linked_ckr`, `source_dkr_checkpoint`, or `contribution_metric`. They still have a
done check, progress signals, and a hand-back rule.
