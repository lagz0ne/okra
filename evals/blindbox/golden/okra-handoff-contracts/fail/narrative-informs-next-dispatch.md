# Narrative Informs Next Dispatch

Worker progress in `progress.jsonl`, check-ins, metric reads, flags, and accepted checkpoints
influence the next dispatch. Freeform narrative is not evidence, but it informs the next dispatch as
context.
