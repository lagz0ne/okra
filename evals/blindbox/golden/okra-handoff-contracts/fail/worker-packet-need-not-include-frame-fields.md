# Worker Packet Need Not Include Frame Fields

Worker prompt packet required fields include objective. `anti_goals`, `action_envelope`, and human
boundaries need not be included.

Current state fields, previous DKR checkpoint fields, assignment fields, budget and stop rule,
hand-back rule, and output schema appear elsewhere in the packet.
