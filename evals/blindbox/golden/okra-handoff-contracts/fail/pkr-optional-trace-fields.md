# PKR Optional Trace Fields

CKRs are measurable contribution context, not worker work, and never dispatched.

PKRs say `linked_ckr`, `source_dkr_checkpoint`, and `contribution_metric` are optional. They still
have a done check, progress signals, and a hand-back rule.
