# Auto-Promoted With Boundary Token

Accumulated anti-goals from previous runs are automatically promoted into the next frame before the
current run has evidence.

The artifact also says `unratified_memory_promotion_count == 0` and mentions human ratification in a
different section, but the automatic promotion rule above still makes prior-run memory hidden
authority.
