# Missing Prompt Packet

The loop has DKR, CKR, and PKR. The orchestrator should continue from previous work and ask the next
worker to keep going. It will use the current objective and avoid anti-goals.

The artifact does not define a worker prompt packet, dispatch packet, current state fields, previous
checkpoint fields, budget, stop rule, hand-back rule, or output schema.
