# PKR Do Not Require Trace Fields

CKRs are measurable contribution context, not worker work, and never dispatched.

PKRs do not require `linked_ckr`, `source_dkr_checkpoint`, or `contribution_metric`. They still have
a done check, changed paths, commands run, and progress signals.
