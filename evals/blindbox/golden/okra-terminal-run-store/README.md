# OKRA Terminal Run Store Calibration

`okra_terminal_run_store.py --calibrate` builds temporary hash-chained pass/fail stores at runtime.
This directory exists so the shared validation loop can treat the checker like the other calibrated
blindbox gates.
