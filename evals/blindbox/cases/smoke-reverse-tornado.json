{
  "id": "smoke-reverse-tornado",
  "description": "A minimal blindbox case that checks whether an agent can use the skill to produce a Reverse Tornado loop from a sparse repo.",
  "skill": "reverse-tornado-okr",
  "fixture": "simple-repo",
  "prompt": "Use the available Reverse Tornado OKR skill to inspect this repository and create tasks/reverse-tornado-loop.md. Keep the loop grounded in the files present in the repo. Treat it as a delegated loop: DKR learning checkpoints must come before CKR/PKR promotion, each DKR must name the steering decision it unlocks and the risk or anti-goal uncertainty it reduces, CKR remains measurable context, DKR and PKR are worker scopes, and long-running workers use ten-minute heartbeat check-ins with file-based progress reports under `.okra/runs/<run-id>/workers/<worker-id>/progress.jsonl`. Do not implement product code or create anything outside `.okra/` and `tasks/reverse-tornado-loop.md`.",
  "allowed_paths": [
    ".okra/",
    "tasks/reverse-tornado-loop.md"
  ],
  "checks": [
    {
      "type": "file_exists",
      "path": "tasks/reverse-tornado-loop.md"
    },
    {
      "type": "file_contains",
      "path": "tasks/reverse-tornado-loop.md",
      "contains": [
        "18%",
        "35%",
        "12 per 100",
        "first-run"
      ]
    },
    {
      "type": "reverse_tornado_loop",
      "path": "tasks/reverse-tornado-loop.md"
    },
    {
      "type": "okra_hard_gates",
      "path": "tasks/reverse-tornado-loop.md"
    }
  ]
}
